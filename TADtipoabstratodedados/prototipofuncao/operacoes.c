#include <stdio.h>
#include "operacoes.h"


    
    int main(){

    int a = 10, b = 20;

    printf("Soma de a+b = %d\n", soma(a,b));
    printf("Subtração de a - b = %d\n", subtracao(a,b));
    printf("Produto de a * b = %d\n", mult(a,b));
    return 0;
  
}

